name: drift

on:
  pull_request:
    paths-ignore:
      - '**/*.png'
      - '**/*.jpg'
      - '**/*.jpeg'
      - '**/*.svg'
      - '**/*.gif'
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Run drift detectors
        uses: alawein/repo-drift@c9937d8df883da3f1561361e7b006201d380f07b # v0.1.0
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
